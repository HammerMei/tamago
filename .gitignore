node_modules/
__pycache__/
*.pyc
.DS_Store
.coverage
.pytest_cache/
# Profile repos cloned alongside tamago (each has its own .git)
*-profile/
# Memory files belong in profile repos, never in tamago engine repo
agents/memory/
.claude
.opencode
# Machine-local install state (generated by `tamago install`) — gitignored
# .tamago/tamago.conf is user-authored and SHOULD be committed
machine.env
.tamago/machine.env
.tamago/machine.toml
